import actionTypes from '../actions'

const introReducer = (state = {
  visible: false
}, action) => {
  switch(type) {
    case LOGIN_MODAL:
      return {
        
      }
    case REGIS_MODAL:
      return {

      }
  }
}